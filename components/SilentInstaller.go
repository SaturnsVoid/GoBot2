//Silent Installers
package components

func msiIntaller(url, md5, args string) { //basic MSI silent installer
	//Download the Installer
	//Run it using the /quiet
	//Allow use of MD5 check and extra args
}

func netInstaller(url, md5, args string) { //dotNET framework silent installer
	//Download the Installer
	//Run it using the /q /norestart
	//Allow use of MD5 check and extra args
}
